# Homework 02 — Tooling Setup

This workspace practices the canonical project scaffold, a Python 3.11 environment, ignored `.env` configuration, reusable config helpers, a pinned dependency file, and an executable Jupyter environment check. The real ongoing implementation is maintained separately in `project/`.

AI assistance was used to assemble and verify the course scaffold; Hanson Sun reviewed the completed work.
